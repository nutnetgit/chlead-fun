# สถานะงาน Ch.Lead FUN — ปรับล่าสุด 2026-09-11

ไฟล์นี้คือจุดเริ่มสำหรับเซสชันใหม่ โดยเฉพาะเซสชันบน cloud ที่เห็นเฉพาะ repo
ไม่เห็นเครื่อง NAS และไม่เห็น source ของ SPS ให้ถือว่าไฟล์นี้คือความจริงล่าสุด
แล้วอัปเดตทุกครั้งที่มีการ deploy

## กำลังรันจริงอยู่ตอนนี้

- image `3ac78b5969a6` สร้างเมื่อ 2026-09-09 23:04 ตรงกับ commit `c8eccd4`
- คอนเทนเนอร์ `fun-app` บน NAS ปกติ log สะอาด
- migration ที่ลงฐานแล้ว: ถึง `sql/035`

## commit ที่ push แล้วแต่ยังไม่ได้ deploy

| commit | เนื้อหา | ต้องทำอะไรก่อนขึ้น |
|---|---|---|
| `513b979` | ซิงก์รุ่นรถและสีจาก SPS | รัน `sql/036` เพิ่มคอลัมน์ `fun_vehicle_color.color_code` |
| `e081032` | คู่มือส่งมอบให้ Dev SPS | ไม่มี เป็นเอกสารล้วน |

วิธีขึ้น: ไฟล์พร้อมอยู่บน NAS แล้วที่ `/volume1/docker/fun/` สั่ง

```bash
sudo bash /volume1/docker/fun/deploy_036.sh
```

สคริปต์จะรัน SQL ก่อน แล้วค่อย `docker load` และ `compose up -d --force-recreate`
ถ้าขึ้นภาพใหม่แล้วต้องเห็น image id เปลี่ยนเป็น `82097e7bd141`

## ข้อมูลจับคู่กับ SPS

| รายการ | สถานะ |
|---|---|
| `fun_branch.dms_branch_id` | กรอกครบ 9 สาขา · รอยืนยัน 3 รายการ: Mazda นครปฐม ชี้ไป 1 ซึ่งฝั่ง SPS ชื่อ มาสด้า สำนักงานใหญ่ · KIA สามพราน ชี้ไป 9 ซึ่งชื่อ KIA นครปฐม · GAC นครปฐม ชี้ไป 11 ซึ่งไม่พบในข้อมูลชุดที่อ่านมา |
| `fun_brand.dms_brand_id` | **ยังว่างทั้ง 8 ยี่ห้อ** ไม่มี audit log ของการบันทึกเลย แปลว่ายังไม่เคยเซฟสำเร็จ |
| `fun_user.dms_user_id` | ยังไม่ได้กรอก รอรายชื่อ `u_id` จากทีม SPS |

ค่าอ้างอิงของ SPS จาก dump ที่อ่านไว้: Mazda 1, Ford 2, Mitsubishi 3, KIA 13,
GWM 14, Deepal 15, GAC 17, Lepas 21 · ให้ยืนยันกับระบบจริงก่อนใช้

## ค่าที่ยังไม่ได้ตั้งบน NAS

`DMS_MYSQL_URL`, `SSO_API_TOKEN`, `SPS_SSO_LANDING_URL` — ทั้งสามยังว่าง
ผลคือการซิงก์รุ่น/สีปิดอยู่ และปุ่ม "เปิดใบจองใน SPS" ยังไม่แสดง
รายละเอียดว่าใครต้องให้ค่าอะไร อยู่ใน `docs/SPS_INTEGRATION.md` §0 และ §7

## เส้นที่แตะไม่ได้จาก cloud

deploy, migration, อ่านฐานข้อมูลจริง, source ของ SPS ที่ `D:\adamsps`
และโปรเจกต์ CPT ที่ `ClaudeCode\Insurance` ทั้งหมดอยู่บนเครื่องที่บ้าน/สำนักงาน
เซสชัน cloud ทำได้แค่แก้โค้ดกับเอกสาร แล้ว push
