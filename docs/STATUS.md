# สถานะงาน Ch.Lead FUN — ปรับล่าสุด 2026-09-11

ไฟล์นี้คือจุดเริ่มสำหรับเซสชันใหม่ โดยเฉพาะเซสชันบน cloud ที่เห็นเฉพาะ repo
ไม่เห็นเครื่อง NAS และไม่เห็น source ของ SPS ให้ถือว่าไฟล์นี้คือความจริงล่าสุด
แล้วอัปเดตทุกครั้งที่มีการ deploy

## กำลังรันจริงอยู่ตอนนี้

- image `82097e7bd141` สร้างเมื่อ 2026-09-10 12:13 โค้ดตรงกับ commit `513b979`
  ซึ่งรวมงานซิงก์รุ่นรถและสีแล้ว
- คอนเทนเนอร์ `fun-app` บน NAS ปกติ ตัวจับเวลาลงทะเบียนงานซิงก์เรียบร้อย
- migration ที่ลงฐานแล้ว: ถึง `sql/036` ครบ ไม่มีตัวค้าง
- commit หลังจากนั้นเป็นเอกสารล้วน ไม่ต้อง deploy

## ข้อมูลจับคู่กับ SPS

| รายการ | สถานะ |
|---|---|
| `fun_branch.dms_branch_id` | ครบ 9 สาขา |
| `fun_brand.dms_brand_id` | ครบ 8 ยี่ห้อ · Mazda 1, Ford 2, Mitsubishi 3, KIA 13, GWM 14, Deepal 15, GAC 17, Lepas 21 |
| `fun_user.dms_user_id` | ยังไม่ได้กรอก รอรายชื่อ `u_id` จากทีม SPS |

รอยืนยันกับระบบจริง 3 สาขา เพราะชื่อไม่ตรงกันเป๊ะกับ dump ที่ใช้เทียบ
Mazda นครปฐม ชี้ไปสาขา 1 ซึ่งฝั่ง SPS ชื่อ มาสด้า สำนักงานใหญ่ ·
KIA สามพราน ชี้ไปสาขา 9 ซึ่งชื่อ KIA นครปฐม ·
GAC นครปฐม ชี้ไปสาขา 11 ซึ่งไม่พบใน dump ที่อ่านมา

## ค้างอยู่เรื่องเดียว

`DMS_MYSQL_URL` ยังไม่ได้ตั้ง รอบัญชี MySQL สิทธิ์ `SELECT` อย่างเดียวบนฐาน SPS
จากฝ่าย IT · ผลตอนนี้คือการซิงก์รุ่น/สีปิดอยู่ หน้า ตั้งค่า › รุ่นรถและสี
จะขึ้นว่ายังไม่ได้ตั้งค่าการเชื่อมฐาน ซึ่งเป็นพฤติกรรมที่ถูก
คำสั่งสร้างบัญชีและรายชื่อตารางที่ต้องเปิดสิทธิ์อยู่ใน `docs/SPS_INTEGRATION.md` §5.1

## รอฝั่ง SPS

`SSO_API_TOKEN` กับ `SPS_SSO_LANDING_URL` ยังว่าง เพราะรอไฟล์ `sso_land.php`
ที่ทีม DMS ต้องเขียน · ผลคือปุ่ม "เปิดใบจองใน SPS" ยังไม่แสดง
เซลส์คีย์ใบจองใน SPS เองตามปกติไปก่อน
รายละเอียดว่าใครต้องทำอะไร อยู่ใน `docs/SPS_INTEGRATION.md` §0

## เส้นที่แตะไม่ได้จาก cloud

deploy, migration, อ่านฐานข้อมูลจริง, source ของ SPS ที่ `D:\adamsps`
และโปรเจกต์ CPT ที่ `ClaudeCode\Insurance` ทั้งหมดอยู่บนเครื่องที่บ้าน/สำนักงาน
เซสชัน cloud ทำได้แค่แก้โค้ดกับเอกสาร แล้ว push
